import App from './App.svelte';

// const app = App.create({
// 	target: document.getElementById('app') // Ensure this matches the HTML element with id="app"
//   });

//   export default app;

App.render({
	target: document.getElementById('root'),
});